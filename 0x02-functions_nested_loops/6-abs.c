#include <stdio.h>
#include <stdlib.h>
#include "main.h"
/**
 * _abs - absulte value
 *@n: input
 *
 * Return: return +
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (abs(n));
	}
	else
	{
		return (n);
	}
}

